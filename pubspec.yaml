name: school_tracker
description: A Flutter project for tracking schools, vehicles, and students.

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.19.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^0.13.6
  shared_preferences: ^2.1.0
  image_picker: ^1.0.4
  app_links: ^6.1.0   # Modern deep link handler compatible with AGP 8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
